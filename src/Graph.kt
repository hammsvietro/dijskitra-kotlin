import kotlin.collections.MutableList

class Graph constructor(val valueMatrix: List<List<Int>>) {

    init {
        val vertices: MutableList<Vertex> = mutableListOf()
        for (i in 0..valueMatrix.size) {

        }
    }
}